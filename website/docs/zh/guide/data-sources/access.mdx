---
title: 访问与传输
description: 文件关联、SSH 隧道与连接迁移。
---

# 访问与传输

## 文件关联

在操作系统中直接打开 `.duckdb` 或 `.parquet` 文件即可启动 Duckling——无需先打开应用。

## SSH 隧道

适用于 **MySQL / PostgreSQL**，可使用**可复用的 SSH 配置**（在设置中配置）或手动配置。

## 连接传输

将**一个或所有连接**导出为 JSON，可选**主密码**加密密钥。在任意机器上导入该 JSON 即可恢复连接。
