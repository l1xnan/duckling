---
title: 数据库服务器
description: MySQL、PostgreSQL、ClickHouse 与实验性的 Doris / StarRocks 连接器。
---

# 数据库服务器

在侧边栏中配置服务器数据库的连接信息。

## MySQL

主机 / 端口 / 数据库 / 用户名 / 密码。

## PostgreSQL

主机 / 端口 / 数据库 / 用户名 / 密码，外加 **SSL 模式**选择。

## ClickHouse

通过 **HTTP 接口**（端口一般为 `8123`）连接，填写主机 / 端口 / 数据库 / 用户名 / 密码。

## Doris / StarRocks

实验性，未充分测试。通过 **MySQL 协议**连接，端口默认为 `9030`。
