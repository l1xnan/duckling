---
title: 侧边栏
description: 数据库资源管理器、查询历史与收藏，均支持搜索。
---

# 侧边栏

侧边栏（按 `Mod+B` 切换）提供：

- **数据库资源管理器** — 浏览所连数据源的数据库、模式、表与列。
- **查询历史** — 之前运行的查询，可搜索。
- **收藏** — 置顶表与已保存 SQL，一键访问，可搜索。

右键连接可**重命名**（`F2`）、编辑**属性**（`F3`）、打开新 **SQL 编辑器**（`F4`）、**刷新**（`F5`）或**删除**（`Delete`）。
